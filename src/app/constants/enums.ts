export enum EndPoint {
  TODO = 'todo',
  WEATHER = 'weather',
  HOME = 'home',
  SETTINGS = 'settings',
}

export enum Units {
  METRIC = 'metric',
  STANDARD = 'standard',
  IMPERIAL = 'imperial',
}

export enum TempUnits {
  CELSIUS = '℃',
  FAHRENHEIT = 'F',
  KELVIN = 'K',
}
